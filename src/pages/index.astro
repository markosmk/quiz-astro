---
import { QuizApp } from '@/components/QuizApp';
import Layout from '@/layouts/Layout.astro';

const { pathname } = new URL(Astro.request.url)
---

<Layout title="Welcome to Quiz-Astro.">
	<QuizApp {pathname} client:only />
</Layout>

